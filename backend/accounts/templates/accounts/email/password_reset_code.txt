{% if display_name %}Hi {{ display_name }},{% else %}Hi,{% endif %}

Someone (hopefully you) asked to reset the password on your TimeLine account.
Enter this code to choose a new password:

    {{ code }}

The code expires in {{ expiry_minutes }} minutes.

If you didn't ask to reset your password, you can safely ignore this email —
your password stays exactly as it is, and no one can change it without this code.

— TimeLine
